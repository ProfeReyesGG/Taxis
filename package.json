{
  "name": "taxi-turicato-netlify",
  "version": "2.2.0",
  "private": true,
  "type": "module",
  "description": "Sitios y taxis verificados con modo oscuro, pruebas integrales persistentes, reinicio maestro, viajes compartidos y control por base.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node --check netlify/functions/taxi.mjs",
    "check": "node --check netlify/functions/taxi.mjs"
  },
  "dependencies": {
    "@netlify/blobs": "^10.7.13"
  }
}
